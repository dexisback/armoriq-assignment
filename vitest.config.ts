import { defineConfig } from "vitest/config";

// Workspace projects are defined in ./vitest.workspace.ts
// This file exists so `vitest` invoked at the repo root uses the workspace.
export default defineConfig({});
